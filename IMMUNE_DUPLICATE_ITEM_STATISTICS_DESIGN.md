# 免疫重复项目统计设计文档

本文档记录 `统计分析管理 -> 免疫重复项目统计` 的功能口径和分阶段实现方案。当前已完成阶段 1 的 Win32 页面骨架，并完成阶段 2 的住院项目配置、只读查询核心和页面结果接入；全程不写数据库。

## 当前状态

- 页面入口：`统计分析管理 -> 免疫重复项目统计(&3)`。
- 页面性质：只读统计页，不写入 LIS 业务表。
- 核心代码：
  - `src/immune_duplicate_statistics_module.h`
  - `src/immune_duplicate_statistics_module.cpp`
- 第一阶段状态：已接入菜单和 Win32 MDI 页面骨架，默认时间、汇总 ListView、明细 ListView、空结果状态、本地排序/复制入口和跳转预留已完成；顶部采用两行筛选布局并按当前字体动态测量标签宽度。
- 第二阶段状态：已接入 `YJ_ZYSQ + ZY_INPATIENT + JC_DEPT_PROPERTY` 真实只读查询，按 `SQNR` 识别基准和疑似重复医嘱，按 `INPATIENT_ID` 聚合同一次住院，并由后台线程填充汇总和明细。
- 尚未接入：CSV 明细导出、基于 `LS_AS_BARCODE.BEDNO` 的床号回填、LIS 报告结果字段和真实报告跳转数据。
- 验证状态：MinGW Windows 交叉编译通过；真实数据库命中结果仍需在 Windows 现场环境核对。

## 既有模块核实结果

阶段 2 口径已与项目现有模块和建表脚本交叉核实：

| 字段/关系 | 既有项目用法 | 本模块结论 |
| --- | --- | --- |
| `YJ_ZYSQ.SQRQ` | 标本签收中心补查住院医嘱时映射为申请时间 | 作为申请时间展示，不作为第一版筛选时间 |
| `YJ_ZYSQ.BBCJSJ` | 标本签收中心映射为采集时间 | 作为采集时间展示 |
| `YJ_ZYSQ.JSSJ` | 标本签收中心映射为签收时间，并用于“条码已存在”的签收提示 | 确认为页面签收时间和查询时间字段 |
| `YJ_ZYSQ.SQNR` | 标本签收中心映射为医嘱内容 | 用于基准和重复医嘱关键字匹配 |
| `YJ_ZYSQ.TXM` | 标本签收中心按条码精确补查 | 确认为基准和重复条码字段 |
| `LS_AS_BARCODE.BARCODE / BEDNO` | 已签收条码查询、标本签收中心和急诊样本统计均直接展示条码表 `BEDNO` | 后续按基准申请 `TXM -> BARCODE` 回填床号，不新增未经项目验证的床位字典关联 |
| `YJ_ZYSQ.BSCBZ` | 现有住院、门诊医嘱补查均使用 `NULL/0` 过滤 | 沿用为有效行过滤惯例，但不扩展解释其业务状态 |
| `YJ_ZYSQ.JSKS` | 标本签收中心映射为检验室/专业组代码 | 需要展示接收检验室时使用 |
| `YJ_ZYSQ.SQKS` | 门诊查询对同名字段按申请科室处理并关联 `JC_DEPT_PROPERTY.DEPT_ID` | 住院申请科室沿用相同字典口径 |
| `YJ_ZYSQ.ZXKS` | 门诊查询对 `YJ_MZSQ.ZXKS` 使用 `102/401` 白名单；住院字段含义尚无既有模块命名依据 | 住院一期强制限定 `ZXKS IN (102,401)`，暂不展示名称或解释代码含义 |
| `YJ_ZYSQ.YJSQID` | 建表脚本定义为聚集主键 | 用作申请明细稳定去重键 |
| `ZY_INPATIENT.INPATIENT_ID` | 建表脚本定义为聚集主键，`YJ_ZYSQ` 存在同类型字段和索引 | 按已确认业务关系关联同一次住院 |
| `ZY_INPATIENT.INPATIENT_NO` | 输血结果模块按住院号查询，表触发器同步到 LIS `REG_NO` | 确认为页面病人号，并可用于后续关联 LIS 报告 |
| `ZY_INPATIENT.SOCIAL_NO` | 输血结果模块用它归并同一身份证下的多次住院号 | 本模块不使用，避免跨住院次数合并 |

项目中目前没有其他模块直接执行 `YJ_ZYSQ.INPATIENT_ID = ZY_INPATIENT.INPATIENT_ID` 联查；该关系来自已确认业务口径，并且两表字段类型、索引和 `ZY_INPATIENT` 主键结构一致。第一版应使用 `INPATIENT_ID` 精确关联，不按姓名、身份证或模糊住院号替代。

## 背景

临床医生会给部分患者开具 `输血常规检查` 医嘱。该医嘱对应免疫类定量项目，已经能够覆盖输血常规相关检测需求。

现场存在重复开单场景：

- 同一患者已经开具并签收 `输血常规检查` 条码。
- 医生又额外开具输血常规定性检查。
- 或者单独开具某一项与输血常规覆盖范围重复的定性检查。

这些重复项目不一定在同一个条码上，可能分布在同一患者同一时间段内的其他条码、其他报告或其他医嘱中。新统计模块的目标是帮助用户按时间段筛出这些疑似重复项目，便于人工核对和后续管理。

## 功能目标

- 用户指定签收时间段。
- 查询该时间段内所有已签收且包含 `输血常规检查` 的条码。
- 以这些申请对应的 `INPATIENT_ID` 为同一次住院主键，继续查询该次住院内是否存在其他疑似重复医嘱；页面通过 `ZY_INPATIENT.INPATIENT_NO` 展示病人号。
- 将“基准输血常规条码”和“疑似重复项目”成组展示，便于核对是否重复开单。
- 结果只做统计和展示，不自动取消医嘱、不写 LIS 业务表。
- 支持导出当前结果，后续可用于科室沟通或人工复核。

## 页面入口

建议入口：

```text
统计分析管理 -> 免疫重复项目统计(&3)
```

理由：

- `HIV 抗体检测统计(&1)` 已使用统计分析入口 1。
- `急诊样本统计(&2)` 已使用统计分析入口 2。
- `质控分析` 已移至 `检验管理`。
- `统计分析3` 当前可作为该模块的自然占位。

## 页面性质

- 只读统计页。
- 不写入 LIS 业务表。
- 默认不自动查询，等待用户点击 `查询`。
- 查询在后台线程执行，避免阻塞 Win32 MDI 主界面。
- 明细填充使用 `WM_SETREDRAW` 暂停重绘后统一刷新，沿用既有统计页的 ListView 性能策略。

## 第一版范围

第一版聚焦“发现疑似重复”，不做自动判断处置：

- 只查已签收条码。
- 只统计当前用户选择时间段内，`YJ_ZYSQ.JSSJ` 已有签收时间且命中基准医嘱的条码。
- 疑似重复项目默认也限定在同一时间段内查找。
- 仅处理住院申请，按同一 `INPATIENT_ID` 匹配，可跨条码发现疑似重复医嘱。
- 同一条码内如果同时存在基准项目和疑似重复定性项目，也应显示为疑似重复。
- 不追溯历史长期重复，不跨很长时间窗口判断。
- 不根据收费、退费或医嘱取消状态做复杂财务判断。

后续如现场确认需要，可增加“重复项目查询窗口”：

```text
仅同一时间段 / 基准签收前后 N 天 / 同一次就诊
```

## 术语

| 名称 | 含义 |
| --- | --- |
| 基准条码 | 指定时间段内已签收，且 `YJ_ZYSQ.SQNR` 精确命中基准医嘱的条码 |
| 基准项目 | 医嘱内容包含 `输血前常规检查` 的申请 |
| 疑似重复项目 | 同一 `INPATIENT_ID` 下，`SQNR` 包含 `乙肝三对` 或 `梅毒` 的申请 |
| 重复组 | 同一次住院下，一个或多个基准条码与其疑似重复医嘱组成的核对单元 |
| 定量项目 | `输血常规检查` 中已有明确数值结果的项目 |
| 定性项目 | 结果通常为阴性、阳性、弱阳性、凝集、未检出等文本判断的项目 |

## 项目配置

第一版没有稳定的项目代码口径，使用 `YJ_ZYSQ.SQNR` 进行医嘱内容匹配。匹配文本集中定义在 C++ 配置结构中，由查询代码生成参数或转义后的只读 SQL 条件，避免散落在多段 SQL 中。

### 基准项目配置

用于识别 `输血常规检查`：

| 配置项 | 说明 |
| --- | --- |
| 医嘱关键字 | `输血前常规检查` |
| 匹配方式 | `SQNR LIKE '%输血前常规检查%'` |
| 数据来源 | `YJ_ZYSQ.SQNR` |
| 适用范围 | 住院申请 |

### 疑似重复项目配置

用于识别定性重复项目：

| 配置项 | 说明 |
| --- | --- |
| 医嘱关键字 | `乙肝三对`、`梅毒` |
| 匹配方式 | `SQNR` 包含任一启用关键字 |
| 重复类别 | 当前统一标记为 `定性项目`，同时保留实际命中的关键字 |
| 是否启用 | 配置项保留启用标志，便于后续关闭误判规则 |

### 推荐配置来源

第一版可以先在 C++ 代码中定义只读数组：

```text
base_order_keywords[]
duplicate_order_keywords[]
```

后续版本再迁移到本机 SQLite 或系统设置页，避免每次调整项目代码都需要重新编译。

## 查询口径

### 时间口径

筛选区使用住院申请签收时间：

```text
YJ_ZYSQ.JSSJ
```

默认范围：

- 开始：当天 `00:00`
- 结束：当天 `23:59`

结束条件沿用现有页面习惯：

```text
JSSJ >= 开始时间
AND JSSJ < DATEADD(minute, 1, 结束时间)
```

### 已签收口径

基准条码必须满足：

```text
YJ_ZYSQ.JSSJ IS NOT NULL
AND (YJ_ZYSQ.BSCBZ IS NULL OR YJ_ZYSQ.BSCBZ = 0)
```

`BSCBZ` 的处理沿用项目现有 `YJ_ZYSQ` 和 `YJ_MZSQ` 医嘱补查逻辑。现有代码只证明这是项目采用的有效行过滤惯例，不能据此断言字段的完整业务含义。`BJSBZ`、退费和取消医嘱状态的业务含义尚未确认，当前查询不擅自增加过滤条件。

### 患者匹配口径

第一版就诊匹配键：

```text
YJ_ZYSQ.INPATIENT_ID = ZY_INPATIENT.INPATIENT_ID
```

说明：

- `INPATIENT_ID` 用于限定同一次住院，是查询和聚合主键。
- `ZY_INPATIENT.INPATIENT_NO` 是页面展示的病人号，不作为同次住院的替代关联键。
- 第一版暂不查询 `YJ_MZSQ`，因此不处理门诊号。
- 不使用姓名、床号、身份证等弱匹配字段，避免误合并不同患者或不同住院次数。

后续扩展门诊时，应单独设计 `YJ_MZSQ` 的门诊就诊键，不能复用住院 `INPATIENT_ID` 口径。

### 基准条码识别

在指定签收时间段内直接从 `YJ_ZYSQ` 识别基准申请：

```sql
JSSJ >= ?
AND JSSJ < DATEADD(minute, 1, ?)
AND SQNR LIKE '%输血前常规检查%'
AND ZXKS IN (102, 401)
AND (BSCBZ IS NULL OR BSCBZ = 0)
```

同一条码有多条申请时，只要任一有效申请命中基准医嘱，该 `TXM` 就作为基准条码。`TXM` 为空的命中记录计入异常，不作为可展示基准条码。

### 疑似重复项目识别

对基准申请得到的 `INPATIENT_ID` 集合，查询同一时间段内同次住院的其他有效申请：

```text
INPATIENT_ID IN (基准住院标识集合)
AND JSSJ 在用户选择时间段内
AND ZXKS IN (102, 401)
AND (SQNR LIKE '%乙肝三对%' OR SQNR LIKE '%梅毒%')
```

疑似重复项目可来自：

- 同一条码的其他住院申请。
- 同一 `INPATIENT_ID` 下的其他条码申请。

排除规则：

- 基准医嘱本身不作为重复项目显示。
- `JSSJ` 为空的未签收申请默认排除。
- `BSCBZ` 非 0 的申请默认排除。
- 同一 `INPATIENT_ID + YJSQID` 只显示一次。

## 数据字段

### YJ_ZYSQ

| 字段 | 用途 |
| --- | --- |
| `YJSQID` | 申请主键，也是重复明细稳定去重键 |
| `INPATIENT_ID` | 关联 `ZY_INPATIENT`，限定同一次住院 |
| `TXM` | 条码号，基准条码和重复条码展示 |
| `SQNR` | 医嘱内容，用于基准和重复关键字匹配 |
| `SQRQ` | 申请时间，页面可作为辅助展示 |
| `JSSJ` | 签收时间和筛选时间 |
| `BBCJSJ` | 采集时间，沿用标本签收中心字段映射 |
| `SQR` | 申请医生代码，后续需要时关联字典展示 |
| `SQKS` | 申请科室代码，按 `JC_DEPT_PROPERTY.DEPT_ID` 映射名称 |
| `ZXKS` | 执行科室相关代码；第一版仅纳入 `102/401`，具体名称待确认 |
| `JSKS` | 接收检验室/专业组代码，沿用标本签收中心字段映射 |
| `BBMC` | 标本名称 |
| `BSCBZ` | 现有医嘱补查采用的有效行过滤字段，完整业务语义待确认 |
| `BJSBZ` | 状态字段，业务语义确认前不参与过滤 |
| `YZID` | 医嘱标识，可用于后续业务核对 |

### ZY_INPATIENT

| 字段 | 用途 |
| --- | --- |
| `INPATIENT_ID` | 与 `YJ_ZYSQ.INPATIENT_ID` 关联 |
| `INPATIENT_NO` | 页面展示病人号 |
| `NAME` | 姓名展示 |
| `SEX` | 性别展示 |
| `IN_DEPT` / `DEPT_ID` | 入院/当前科室代码，不替代申请表 `SQKS` |
| `BED_ID` | 住院记录中的床位关联键；项目现有模块未使用该字段关联床位字典，本模块当前不使用 |
| `IN_DATE` / `OUT_DATE` | 住院起止时间，辅助核对同次住院 |

第一版查询不依赖 `LS_AS_REPORT` 和 `LS_AS_REPENTRY`。报告结果、审核、发送和常规报告跳转需要额外建立 `TXM` 到 LIS 报告的关联，留到后续增强阶段。

## 查询流程

### 阶段 1：查询基准条码

输入：

- 签收开始时间
- 签收结束时间
- 可选院区或科室筛选，第一版可不做

输出：

- 基准条码列表
- 基准病人号集合

伪流程：

```text
query YJ_ZYSQ by JSSJ and ZXKS in (102, 401)
match SQNR against base_order_keywords
join ZY_INPATIENT by INPATIENT_ID
group by TXM
collect INPATIENT_ID
```

### 阶段 2：查询疑似重复项目

输入：

- 阶段 1 得到的 `INPATIENT_ID` 集合
- 同一签收时间段
- 疑似重复项目配置

输出：

- 疑似重复项目明细

伪流程：

```text
query YJ_ZYSQ where INPATIENT_ID in base_inpatient_id_set
filter by the same JSSJ range, ZXKS whitelist and valid-row rule
match SQNR against duplicate_order_keywords
deduplicate by INPATIENT_ID + YJSQID
```

### 阶段 3：内存聚合

按同一次住院聚合：

```text
INPATIENT_ID
  INPATIENT_NO
  base barcode(s)
  duplicate order(s)
```

只保留存在疑似重复医嘱的住院组。可提供 `显示无重复基准条码` 复选框作为后续扩展，第一版默认不显示无重复项。

## 第一阶段 Win32 界面设计

第一阶段先实现 Win32 原生 MDI 页面，目标是让用户可以稳定完成“按时间查询 -> 查看重复明细 -> 复制或导出 -> 必要时跳转常规报告”的闭环。该阶段不引入复杂配置页、不做多视图看板，也不做自动处置。

### 设计原则

- 页面保持现有统计模块风格，使用原生 Win32 控件。
- 顶部筛选区紧凑排列，避免做成说明式页面。
- 汇总区和明细区都用 `ListView`，便于复用排序、复制、导出和 `WM_SETREDRAW` 优化策略。
- 页面默认不自动查询，用户点击 `查询` 后后台执行。
- 查询过程中保留上一轮结果，完成后整体替换，减少空白闪烁。
- 所有判断结果都作为“疑似重复”展示，不在界面提供取消、忽略、回写 LIS 等操作。

### 第一阶段页面布局

Win32 MDI 子窗口建议分为三段：

```text
┌────────────────────────────────────────────┐
│ 筛选区第 1 行：签收时间起止              │
│ 筛选区第 2 行：查询 / 导出 / 状态         │
├────────────────────────────────────────────┤
│ 汇总 ListView：基准条码数、重复患者数等    │
├────────────────────────────────────────────┤
│ 明细 ListView：按疑似重复项目逐行展示      │
└────────────────────────────────────────────┘
```

布局要求：

- 筛选区采用两行布局，签收时间标签通过公共 `search::measure_control_text_width` 随当前系统字体和 DPI 动态测量宽度，避免大字号下截断。
- 汇总区高度固定，约 2 行 ListView 高度，类似急诊样本统计。
- 明细区占用剩余空间，随窗口大小伸缩。
- 窗口缩小时优先保证筛选区控件可见，明细区通过横向滚动查看宽列。
- 页面背景、字体和按钮创建方式复用既有统计页面，不额外引入 owner-draw 控件。

### 控件清单

第一阶段控件：

| 控件 | 类型 | 默认值/行为 |
| --- | --- | --- |
| 签收开始时间 | DateTimePicker | 当天 `00:00`，精确到分钟 |
| 签收结束时间 | DateTimePicker | 当天 `23:59`，精确到分钟 |
| 查询 | Button | 后台查询，查询中禁用 |
| 导出CSV | Button | 无结果时禁用；导出当前排序后的明细 |
| 状态文本 | Static | 显示查询中、耗时、基准条码数、异常数量或错误 |
| 汇总列表 | ListView | 两行：表头和数量 |
| 明细列表 | ListView | 疑似重复项目明细 |

第一阶段暂不开放的控件：

| 控件 | 暂缓原因 |
| --- | --- |
| 重复范围下拉 | 先固定同一时间段，避免口径过早发散 |
| 院区/科室筛选 | 先验证重复项目命中质量，再决定是否增加 |
| 项目配置入口 | 先用代码集中配置，待项目代码稳定后再做设置页 |
| 只看已审核重复项目 | 先展示完整疑似重复，再按现场反馈增加筛选 |

### 状态和空结果

状态文本建议：

| 场景 | 文案 |
| --- | --- |
| 初始 | `请选择签收时间后查询` |
| 查询中 | `正在查询免疫重复项目...` |
| 无基准条码 | `该时间段未找到输血常规检查条码` |
| 有基准但无重复 | `已找到 X 个基准条码，未发现疑似重复项目` |
| 有重复 | `基准条码 X 个，疑似重复患者 Y 人，重复项目 Z 项` |
| 时间范围过大 | `查询范围较大，可能耗时较长` |
| 查询失败 | 显示数据库错误摘要，不弹出大量技术细节 |

空结果处理：

- 没有基准条码时清空汇总和明细。
- 有基准但无重复时汇总显示基准条码数和基准患者数，明细为空。
- 查询失败时保留上一轮结果，并在状态文本显示失败原因。

### 第一阶段交互

- 点击 `查询`：禁用查询按钮，后台查询，完成后恢复。
- 点击明细表头：对当前内存结果排序，不重新访问数据库。
- 右键明细单元格：提供 `复制单元格`。
- 双击明细行：优先跳转重复项目所在报告的 `常规报告` 页面；缺少报告号、仪器或日期时提示无法跳转。
- 点击 `导出CSV`：导出当前明细列表当前排序结果。
- `Esc` 不关闭页面，避免误关统计窗口。
- 页面关闭时取消或忽略未完成后台结果，避免向已销毁窗口投递 UI 更新。

### 第一阶段颜色和提示

保持颜色克制：

- 名称兜底命中的重复项目可用浅黄色行背景提示人工核对。
- 同条码重复和跨条码重复不使用强色块，使用 `重复关系` 文本区分。
- 查询错误不使用持续弹窗，优先状态栏文本；只有导出失败等用户动作失败再弹 MessageBox。

## 页面结构

### 筛选区

第一阶段顶部筛选区包含：

- `签收时间` 开始时间，精确到分钟。
- `签收时间` 结束时间，精确到分钟。
- `查询`。
- `导出CSV`。
- 状态文本。

`重复范围` 第一阶段固定为 `同一时间段`，不做可编辑控件；`只看已审核重复项目`、院区和科室筛选作为后续扩展。

### 汇总区

上方汇总区使用横向 `ListView`，指标：

| 指标 | 口径 |
| --- | --- |
| 基准条码数 | 命中 `输血常规检查` 的唯一条码数 |
| 基准患者数 | 基准条码涉及的唯一 `INPATIENT_ID` 数，界面沿用“患者数”名称 |
| 疑似重复患者数 | 同一 `INPATIENT_ID` 下存在疑似重复医嘱的住院次数 |
| 疑似重复条码数 | 命中疑似重复项目的唯一条码数 |
| 疑似重复项目数 | 按 `YJSQID` 去重后的重复医嘱明细数 |
| 同条码重复数 | 基准条码内命中疑似重复项目的数量 |
| 跨条码重复数 | 非基准条码命中疑似重复项目的数量 |

### 明细区

下方主明细 `ListView` 按疑似重复项目逐行展示。建议列：

| 列名 | 来源/口径 |
| --- | --- |
| 病人号 | `ZY_INPATIENT.INPATIENT_NO` |
| 姓名 | `ZY_INPATIENT.NAME` |
| 类型 | 固定显示 `住院` |
| 科室 | 基准申请 `SQKS -> JC_DEPT_PROPERTY.DEPT_ID -> NAME`，匹配不到时回退代码 |
| 床号 | 当前为空；后续按基准申请 `YJ_ZYSQ.TXM -> LS_AS_BARCODE.BARCODE -> BEDNO` 回填 |
| 基准条码 | 命中输血常规的条码 |
| 基准样本号 | 第一版为空，后续关联 LIS 报告后补充 |
| 基准医嘱 | 基准申请 `SQNR` |
| 基准签收时间 | 基准申请 `JSSJ` |
| 重复条码 | 疑似重复项目所在条码 |
| 重复样本号 | 第一版为空，后续关联 LIS 报告后补充 |
| 重复项目代码 | 第一版为空；当前按医嘱文本识别 |
| 重复项目名称 | 重复申请 `SQNR` |
| 重复类别 | 命中的 `乙肝三对` 或 `梅毒` 关键字 |
| 结果 | 第一版为空，后续关联 LIS 报告后补充 |
| 单位 | 第一版为空 |
| 重复签收时间 | 重复申请 `JSSJ` |
| 报告时间 | 第一版为空 |
| 审核 | 第一版为空 |
| 发送 | 第一版为空 |
| 重复关系 | `同条码 / 跨条码` |

交互：

- 支持点击表头本地排序。
- 支持右键复制当前单元格。
- 双击行可跳转 `常规报告` 并定位重复项目所在报告；如果重复项目没有有效报告号，则提示无法跳转。
- 可后续增加双击基准条码跳转基准报告。

## 去重规则

### 基准条码去重

同一条码有多条住院申请命中基准医嘱时：

```text
BARCODE 只算 1 个基准条码
```

基准患者数：

```text
INPATIENT_ID 去重计数
```

### 疑似重复项目去重

重复项目明细去重键建议：

```text
INPATIENT_ID + YJSQID
```

`YJSQID` 是 `YJ_ZYSQ` 主键，因此不需要按项目代码或项目名称做退化去重。如果一个 `SQNR` 同时包含多个重复关键字，同一申请仍只展示一行，重复类别可拼接命中的关键字。

## 排除和异常

第一版需要单独记录但不作为正常重复结果的异常：

| 异常 | 处理 |
| --- | --- |
| 基准申请缺少 `INPATIENT_ID` | 不做同次住院匹配，在状态文本提示数量 |
| `INPATIENT_ID` 无法关联住院信息 | 可参与匹配，但病人号和姓名为空并计入异常 |
| 基准或重复申请缺少条码 | 保留异常计数，不作为可跳转条码展示 |
| 重复医嘱无报告号 | 第一版正常情况，展示条码和 `SQNR`，双击提示暂不可跳转 |
| `SQNR` 关键字误命中 | 结果按关键字来源展示，现场核对后调整集中配置 |

## SQL 策略

第一版仅涉及 `YJ_ZYSQ` 和 `ZY_INPATIENT`，建议使用两段参数化只读查询：

1. 按 `JSSJ`、`ZXKS IN (102,401)` 和基准 `SQNR` 包含匹配，查询基准申请并联查 `ZY_INPATIENT`。
2. 在 C++ 中整理唯一 `INPATIENT_ID` 集合。
3. 按批次查询同一时间段、同一 `INPATIENT_ID` 下命中重复关键字的 `YJ_ZYSQ` 申请。
4. 在 C++ 中按 `INPATIENT_ID` 聚合基准条码和重复医嘱。

批量查询建议：

- `INPATIENT_ID IN (...)` 每批不超过 200，GUID 文本较长，避免 SQL 过长。
- 只取页面所需字段，避免 `SELECT *`。

## 性能边界

需要考虑：

- 时间范围过大时，`YJ_ZYSQ` 已签收申请数量可能很大。
- `INPATIENT_ID IN (...)` 集合过大时，应分批查询。
- 基准和重复医嘱都使用前置通配的 `LIKE`，不能有效利用 `YJ_ZYSQ_sqnr` 索引，需要先用 `JSSJ`、`ZXKS`、`BSCBZ` 和 `INPATIENT_ID` 缩小范围。
- 第一版只查询住院申请，不访问门诊申请表。
- 查询过程中保留上一轮结果，完成后整体替换，减少界面闪烁。

第一版建议限制默认时间为当天，不自动查询。若用户选择超过 31 天，可提示“查询范围较大，可能耗时较长”。

## 导出

第一版提供 CSV 导出：

- UTF-8 BOM。
- 导出当前已加载、当前排序后的明细。
- 文件名建议：

```text
免疫重复项目统计 - yyyyMMdd-HHmm 至 yyyyMMdd-HHmm.csv
```

导出列与界面明细列一致。后续如需要管理报表，可再设计 DOCX 或 XLSX 模板。

## 后续开发拆分

建议分阶段实现，第一阶段先落 Win32 界面，再接真实查询。

### 阶段 1：Win32 页面骨架和静态交互

第一阶段先完成 Win32 原生界面，不接真实 LIS 查询也可以先用空数据结构打通页面生命周期：

- 新增 `src/immune_duplicate_statistics_module.h`。
- 新增 `src/immune_duplicate_statistics_module.cpp`。
- 接入 `统计分析管理 -> 免疫重复项目统计(&3)`。
- 建立 MDI 单实例窗口。
- 完成筛选区、汇总 ListView、明细 ListView、状态文本、按钮布局。
- 完成本地排序、右键复制单元格、导出按钮启用/禁用状态。
- 预留双击跳转常规报告的入口函数，但没有有效报告数据时只提示不可跳转。

阶段 1 验收标准：

- 页面可从菜单打开和重复激活。
- 窗口缩放时筛选区、汇总区、明细区布局稳定。
- 默认时间为当天 `00:00` 到 `23:59`。
- 无结果状态清晰，不崩溃、不访问 LIS。
- 编译通过。

涉及文件：

```text
src/main_frame.cpp
src/immune_duplicate_statistics_module.h
src/immune_duplicate_statistics_module.cpp
CMakeLists.txt
PROJECT_STRUCTURE.md
README.md
CHANGELOG.md
```

### 阶段 2：住院只读查询接入

- [x] 数据范围暂定住院，主表为 `YJ_ZYSQ`。
- [x] 基准医嘱按 `SQNR` 包含 `输血前常规检查` 匹配，不限定后续方法学或组合名称文本。
- [x] 疑似重复医嘱按 `SQNR` 包含 `乙肝三对` 或 `梅毒` 识别。
- [x] 基准和重复医嘱均限定在用户选择的 `JSSJ` 时间段内。
- [x] 基准和重复医嘱均强制限定 `YJ_ZYSQ.ZXKS IN (102,401)`，暂不解释两个代码对应的名称。
- [x] 按 `YJ_ZYSQ.INPATIENT_ID` 限定同一次住院，通过 `ZY_INPATIENT.INPATIENT_NO` 展示病人号。
- [x] 第一版不查询门诊，不做科室或院区筛选。
- [x] 新增查询结构：

```text
ImmuneDuplicateStatQuery
ImmuneDuplicateStatDetailRow
ImmuneDuplicateStatSummary
query_immune_duplicate_statistics
```
- [x] 查询按钮后台执行真实查询，查询期间保留上一轮结果。
- [x] 查询成功后整体替换汇总和明细，失败时保留上一轮结果。
- [x] 基准条码按 `TXM` 去重，患者数按 `INPATIENT_ID` 去重。
- [x] 重复申请按 `YJSQID` 一行一项去重；多个基准条码聚合展示，不重复展开同一重复申请。
- [x] 状态文本区分无基准、有基准无重复和发现重复，并提示无条码基准申请及住院信息未匹配数量。

涉及文件：

```text
src/search_core.h
src/search_core.cpp
src/immune_duplicate_statistics_module.cpp
```

### 阶段 3：导出和报告增强

- CSV 导出。
- 明细双击跳转常规报告。
- 右键复制单元格。
- 查询结果排序和状态栏提示。

## 待确认问题

现场验证和后续增强前仍需确认：

1. `YJ_ZYSQ.BSCBZ = 0` 是否足以代表有效申请；`BJSBZ`、退费或取消医嘱是否需要额外排除。
2. `输血前常规检查` 的包含匹配是否需要排除某些扩展医嘱名称。
3. `乙肝三对`、`梅毒` 的包含匹配是否需要排除某些医嘱名称。
4. 同一次住院存在多个基准条码且 `LS_AS_BARCODE.BEDNO` 不一致时，床号应取当前基准条码值、首个非空值还是合并展示。
5. `YJ_ZYSQ.ZXKS` 的准确业务名称，以及 `102/401` 分别对应什么名称。
6. 后续是否需要补充门诊申请 `YJ_MZSQ`，以及门诊同一次就诊的关联键。

## 当前结论

`免疫重复项目统计` 已完成 Win32 页面骨架和住院一期真实只读查询接入。下一步应先在 Windows 现场数据库核对命中结果，再开发 CSV、按基准条码回填 `LS_AS_BARCODE.BEDNO` 和 LIS 报告关联。整体实现坚持：

- 只读查询。
- 以 `YJ_ZYSQ.JSSJ` 时间段筛选基准和重复申请。
- 基准和重复查询都强制限定 `YJ_ZYSQ.ZXKS IN (102,401)`。
- 以 `INPATIENT_ID` 限定同一次住院，`INPATIENT_NO` 仅用于展示。
- 基准和重复医嘱均按集中配置关键字包含匹配。
- 第一版只做住院，不混入门诊关联口径。
- 结果用于人工核对，不自动修改 LIS 数据。
