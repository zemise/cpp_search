# 常用电话模块设计：RTF + RichEdit 内嵌查看器

## 背景

主程序需要在自定义菜单栏新增一个“常用电话”模块，方便用户在 LIS 工作台内部查看和搜索常用电话号码。该资料由用户自行维护和替换，程序只负责加载展示，不参与编辑。

## 目标

- 在 `工具` 菜单新增 `常用电话` MDI 子窗口，替换原 `工具5` 占位入口。
- 直接在本程序内查看电话文档，不跳转 Word、WPS、浏览器或 PDF 阅读器。
- 支持 Windows 7 运行环境。
- 用户可自行替换文档文件，替换后在页面点击 `刷新` 即可重新加载。
- 支持在 `documents` 目录下保存多个 RTF 文件，并在页面顶部下拉选择。
- 当前选择写入 `ClientConfig.ini` 的 `[PhoneDirectory] SelectedFile`，下次打开保持一致。
- 支持页面内 `上传` RTF 文件，复制到程序目录 `documents` 下并自动选中。
- 搜索框是页面的主输入焦点：打开、激活、切换文件、上传或刷新后都会回到搜索框。
- 输入内容变化时立即从文档开头搜索第一个匹配项；输入停止 1000ms 后自动全选搜索框内容，方便下一次直接覆盖输入且避免连续输入时过早覆盖。
- 支持 `Ctrl+F` 聚焦搜索框、`Enter` / `F3` 查找下一个。
- 页面控件字体跟随系统设置字号变化。

## 文件格式与部署路径

模块采用 RTF 作为第一版标准格式：

- 默认目录：`<程序目录>\documents`
- 默认文件名：`常用电话.rtf`
- 用户维护方式：用 Word/WPS/写字板编辑原始内容，另存为 RTF 后通过页面 `上传`，或直接放入上述目录。
- 程序加载方式：通过 Windows RichEdit 控件 `EM_STREAMIN + SF_RTF` 原样载入。

不直接加载 `.doc` 的原因：

- `.doc` 是旧二进制 Word 格式，Win32 原生控件不能直接内嵌渲染。
- 依赖 Office/WPS 自动化会引入版本、权限、弹窗、进程残留和部署不确定性。
- RTF 可保留表格、字体、字号、加粗、颜色等常用排版，并能由 RichEdit 在 Windows 7 上原生显示。

## 界面

`常用电话` 页面由顶部单行操作区和下方 RichEdit 查看区组成：

- 文件下拉框：列出 `documents` 目录下所有 `.rtf` 文件，选择后立即加载并持久化；宽度保持紧凑，避免挤占搜索框。
- `上传`：选择本机 RTF 文件并复制到 `documents` 目录；若同名文件已存在，确认后覆盖。
- `刷新`：重新扫描 `documents` 目录并重新读取当前选中的 RTF 文件。
- 搜索框：输入关键词。
- RichEdit 查看区：只读、可选择复制、可滚动查看。

## 快捷键

- `Ctrl+F`：聚焦搜索框并选中现有搜索词。
- 输入变化：立即搜索第一个匹配项；输入停止 1000ms 后全选搜索框内容。
- `Enter`：搜索框内查找下一个，完成后全选搜索词。
- `F3`：查找下一个。

## 实现约束

- 使用 `LoadLibraryW(L"Msftedit.dll")` 加载系统 RichEdit 4.1。
- 使用 `MSFTEDIT_CLASS` 创建 RichEdit 控件。
- 使用 `EM_SETREADONLY` 固定为只读查看器。
- 使用 `EM_STREAMIN + SF_RTF` 加载 RTF。
- 使用 `EM_FINDTEXTEXW` 实现查找；输入变化立即触发自动搜索，搜索框全选动作延迟 1000ms 执行，避免连续输入时打断用户。
- 处理 `WM_APP_FONT_CHANGED`，所有顶部控件和 RichEdit 查看区统一套用系统设置字号。
- 不访问 LIS 数据库，不新增业务写入逻辑。
- 不引入 WebView2、PDF 渲染器、Office/WPS 自动化或第三方文档库。

## 异常处理

- 找不到 RTF 文件时，页面展示纯文本提示，说明应放置的路径；页面不显示“已加载”等状态提示。
- RTF 加载失败时，回退以纯文本方式读取展示，避免页面空白。
- `Msftedit.dll` 加载失败时，模块创建失败并提示当前系统 RichEdit 组件不可用。

## 后续可选扩展

- 在安装包中附带 `documents` 示例目录和示例 RTF 文件。
- 增加打印按钮。
- 增加“打开文件”按钮，允许管理员选择非默认路径。
- 如后续确认现场统一支持新版系统，可评估 PDF/WebView2 方案，但不作为 Windows 7 默认路径。
